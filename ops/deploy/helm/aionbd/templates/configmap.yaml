apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aionbd.fullname" . }}-config
  labels:
    {{- include "aionbd.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
